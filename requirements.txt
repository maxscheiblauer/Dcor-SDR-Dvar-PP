# The environment the published numbers were produced in, frozen 2026-08-11.
# Python 3.12.10.  A rerun on other versions is a different measurement: the
# optimisers branch on floating-point comparisons, so the last bits matter.
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
pandas==3.0.3
joblib==1.5.3
dcor==0.7
POT==0.9.6.post1
xlrd==2.0.2
